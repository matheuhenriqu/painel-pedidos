# ==============================================================================
# VARIÁVEIS DE AMBIENTE - CENTRAL DE PEDIDOS & CHATBOT IA
# ==============================================================================
# Copie este arquivo para .env caso deseje configurar variáveis locais.
# NUNCA insira segredos reais ou service roles em repositórios públicos.
# ==============================================================================

# 1. CONFIGURAÇÕES DO CLIENTE FRONTEND (PÚBLICAS)
# Estas chaves são públicas e operam sob as regras de Row Level Security (RLS) do Supabase.
SUPABASE_URL=https://xqmzysjzopyuelvhokwm.supabase.co
SUPABASE_ANON_KEY=sb_publishable_RfIxDPxwUdyzsaXXsa2tIw_XRvfyxAn

# 2. SEGREDOS DA EDGE FUNCTION SUPABASE (CONFIGURAR VIA CLI OU DASHBOARD)
# ATENÇÃO: Estas chaves NUNCA devem ir para o frontend ou repositório Git.
# Configure nos Secrets do Supabase via CLI:
#   npx supabase secrets set GROQ_API_KEY=gsk_sua_chave_aqui
#   npx supabase secrets set ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
GROQ_API_KEY=gsk_exemplo_sua_chave_secreta_groq_aqui
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,https://seu-dominio.vercel.app
